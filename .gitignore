# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Local override files (never commit)
# Prevents accidental commits of developer-specific configurations
*.local.yaml
*.local.yml
*.local.json


# Coverage reports
coverage.html
coverage.out
coverage.xml
*-coverage.html
*-coverage.out
cmd_coverage.out
full_coverage.out
finalizer-coverage.out
finalizer-coverage.html
packages_coverage.out
pkg_coverage.out
work_coverage.out
work-coverage.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build artifacts
dist/
build/
tmp/
bin/
goneat
./goneat

# Go cache directory
.gocache/

# Scratchpad for generated files and testing
.scratchpad/

# Temporary test directories
test_temp/
test_dir/

# Plans folder - ignore all except PROMPT.md
.plans/
!.plans/PROMPT.md

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
.cursor/
*.swp
*.swo
*~

# Claude Code settings
.claude/settings.local.json

# Log files
*.log

# Environment files
.env
.env.local
.env.*.local

# Temporary files
*.tmp
*.temp
.scratchpad/
test-plan.json
# Note: embedded asset mirrors are tracked to support `go install` builds
test-override.md

# User configuration directory (except our own repo config)
# .goneat/

# Backup files in .goneat directory
.goneat/*.backup
