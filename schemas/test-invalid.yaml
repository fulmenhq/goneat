$schema: https://json-schema.org/draft/2020-12/schema
$id: https://example.com/test
$version: 1.0.0
title: Test Schema
type: object
properties:
  test:
    type: invalid_type  # This should be invalid
required:
  - test
additionalProperties: 123  # This should be boolean or object, not number