$schema: http://json-schema.org/draft-07/schema#
type: invalid_type  # invalid: should be object, array, string, etc.
properties:
  items:
    type: array
    items:
      type: string
    maxItems: 2
  numbers:
    type: array
    items:
      type: number
required:
  - items
