$schema: http://json-schema.org/draft-07/schema#
type: object
properties:
  status:
    type: string
    enum:
      - active
      - inactive
      - pending
  priority:
    type: string
    enum:
      - low
      - medium
      - high
required:
  - status
