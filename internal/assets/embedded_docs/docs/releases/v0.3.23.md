# Goneat v0.3.23 — Bulk Validate Suite + Local Schema DX + Offline Refs

**Release Date**: 2025-12-21
**Status**: Draft

## Overview

Goneat v0.3.23 makes schema validation practical at scale:

- bulk validation for large example suites
- friction-free local schema mapping
- offline `$ref` resolution (no schema registry required)

## ✅ What changed

### Validate: new `suite` subcommand (bulk)

`goneat validate suite` validates many YAML/JSON files in one run.

Highlights:

- `--workers` for parallel execution
- `--expect-fail` and `--skip` globs for invalid/taxonomy fixtures
- `--format json` for CI + AI-friendly structured output

### Validate suite: local schema workflow

Two supported ways to route files to local schemas:

- **Recommended (simple)**: `schema_path` shorthand in `.goneat/schema-mappings.yaml`
- **Optional (stable IDs)**: `overrides.path` + `schema_id` for canonical identifiers

Docs include copy/paste manifest examples:

- `docs/user-guide/commands/validate.md`

### Offline `$ref` resolution (friction-free)

- `validate data --ref-dir` handles root-vs-ref-dir duplicate `$id` collisions correctly
- `validate suite --ref-dir` supports offline `$ref` resolution for schema ecosystems before a registry exists

### Docs: release notes available via `goneat docs`

- `goneat docs show release-notes` shows the curated recent release notes.
- `goneat docs show releases/latest` shows the current release note.

---

_Generated by Code Scout ([OpenCode](https://github.com/sst/opencode)) under supervision of @3leapsdave_
